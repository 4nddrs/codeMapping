---
description: Map a target project's training run into this menu
argument-hint: "<target-path>"
---

Follow skill code-mapping. Target: $1

code_flow_v2/ is the rule: read OBJECTIVE.md, code-mapping/SKILL.md,
QUICKSTART.md, PUBLISHING.md, then the phase guides. Install and prove
the target env until training can run; if it fails, try alternatives.
Every question: ask_user_choice (2–4 options), never prose.

Scout once, then you do env + bounded 1-process training capture +
menu_card.py --copy into projects/<slug>/. Do not spawn worker,
reviewer, or oracle. Do not write anything in this menu except the
published page and its card.
