This checkout is the published-mapping menu plus `code_flow_v2/`. When the
user gives a target project path, load skill `code-mapping` and follow
`code_flow_v2/` (OBJECTIVE, code-mapping/SKILL.md, QUICKSTART, PUBLISHING,
then the current phase file). That pack is the method. Do not invent
another.

Install and prove the target env until the project can run; if a recipe
fails, try alternatives, then ask with `ask_user_choice` only. Every
question uses that tool (2–4 options). Never ask in prose.

Scout once, then this parent. No worker/reviewer/oracle. No Gentle AI
clarify/plan/SDD.

Write in this repo only `projects/<slug>/`, the menu card in `index.html`
/ `README.md`, and skill-pack edits the user requested. Never
`artifacts/`, `call_trees/`, `envs/`, or a markdown call tree. Evidence
stays in the target's `docs/code_mapping/`. English artifacts unless the
user asks otherwise.
